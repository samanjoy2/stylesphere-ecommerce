# This file contains environment variables for docker-compose.
# You can change these values to customize your local environment.

# Port mappings for the host machine
# Change these if ports 3000 or 5000 are already in use on your system.
FRONTEND_HOST_PORT=3000
BACKEND_HOST_PORT=5000

# The URL for the backend API, as seen by the frontend container.
# In Docker Compose, services can talk to each other using their service names.
# "http://backend:5000" is the address of the backend service from within the Docker network.
NEXT_PUBLIC_API_URL=http://localhost:5000